apiVersion: v1
kind: Service
metadata:
  name: family-tracker
  namespace: family-tracker
spec:
  selector:
    app: family-tracker
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080

apiVersion: v1
kind: Service
metadata:
  name: duty-cycle
  namespace: duty-cycle
spec:
  selector:
    app: duty-cycle
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080

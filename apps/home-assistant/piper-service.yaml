apiVersion: v1
kind: Service
metadata:
  name: piper
  namespace: home-assistant
  labels:
    app: piper
spec:
  type: ClusterIP
  selector:
    app: piper
  ports:
  - name: wyoming
    port: 10200
    protocol: TCP
    targetPort: wyoming

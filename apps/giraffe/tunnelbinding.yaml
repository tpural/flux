apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: giraffe-binding
  namespace: giraffe
tunnelRef:
  kind: Tunnel
  name: giraffe-tunnel
subjects:
  - kind: Service
    name: giraffe
    spec:
      fqdn: giraffe.tapslab.com
      path: /
      protocol: http
